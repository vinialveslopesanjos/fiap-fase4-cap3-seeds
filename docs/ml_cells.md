# Células ML — colar no notebook (Higor → Humberto)

> Exportar de `ml/classify.py` / `models/classification_metrics.json`.

## Seção — Modelos (tarefa 2)

- Split 70/30 estratificado
- KNN, SVM, Random Forest (+ Naive Bayes, Logistic)
- Tabela: acurácia, precisão, recall, F1

## Seção — Tuning (tarefa 3)

- Grid Search no melhor modelo (`models/model_comparison.csv`)
- Matriz de confusão

_SUBSTITUIR após `python ml/classify.py`._
